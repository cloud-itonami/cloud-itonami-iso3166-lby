{:itonami.blueprint/id "cloud-itonami-iso3166-lby"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Libya"
 :itonami.blueprint/iso3166 "LBY"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "MarketEntry-LLM Advisor ⊣ Market-Entry Compliance Governor, a REAL
 compiled `kotoba-lang/langgraph` StateGraph (`:intake -> :advise ->
 :govern -> :decide -+-> :commit / :request-approval -> :commit /
 :hold`), append-only audit ledger. Five governor checks, in priority
 order, ALL HARD violations a human approver cannot override:
 spec-basis citation, evidence-checklist completeness, an
 independent MAINTAINED BANK-ACCOUNT-BALANCE FLOOR VALIDATION against
 the Ministry of Economy and Trade's own documented LYD 150,000
 foreign-company-representative-office floor (`:office-balance-lyd`,
 the flagship check for this jurisdiction -- a genuinely different
 instrument from Equatorial Guinea's paid-in SHARE-CAPITAL floor for a
 newly-incorporated SARL, sourced directly from Libya's own
 market-entry registrar rather than a secondary multilateral report),
 independent engagement-fee recompute (base + months x rate), double-
 draft guard, double-submit guard, plus a SOFT confidence-floor/
 actuation gate. `:filing/draft` and `:filing/submit` (real portal
 actuation) never auto-commit in any phase; `interrupt-before
 #{:request-approval}` pauses escalated requests for real human
 sign-off via a checkpointer, and BOTH `:commit` and `:hold` durably
 append to the real audit ledger (`store/append-ledger!`). Store has
 a `MemStore` and a `DatomicStore` (via `kotoba-lang/langchain-store`,
 no hand-rolled EDN-blob codec), proven equivalent by a shared
 store-contract test. Unlike the GNQ sibling, this catalog does NOT
 add a tax-identification-number-verification check -- no distinct TIN-
 issuance step was independently confirmed for Libya this iteration
 (honest, disclosed omission, see `marketentry.facts`). 37 tests / 119
 assertions green, clj-kondo 0 errors."}
